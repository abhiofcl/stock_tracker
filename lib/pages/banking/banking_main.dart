import 'package:flutter/material.dart';

class BankingMainScreen extends StatelessWidget {
  const BankingMainScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Banking"),
      ),
    );
  }
}
